import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frenz4u.app.reacaple',
  appName: 'type-cap',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
