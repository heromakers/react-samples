import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frenz4u.app.starter',
  appName: 'IonicCapacitor',
  webDir: 'build'
};

export default config;
