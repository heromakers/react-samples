import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frenz4u.app.ioncample',
  appName: 'ioni-cap',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
