import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frenz4u.app.ioncapql',
  appName: 'ioncapql',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
